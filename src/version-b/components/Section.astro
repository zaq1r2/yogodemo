---
interface Props {
  id?: string;
  class?: string;
}
const { id, class: className = '' } = Astro.props;
---

<section id={id} class:list={['container-wide py-space-5', className]}>
  <div class="max-w-3xl">
    <slot />
  </div>
</section>
