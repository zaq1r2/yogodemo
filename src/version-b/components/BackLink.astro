---
interface Props {
  href: string;
  label?: string;
}
const { href, label = 'Back' } = Astro.props;
---

<a href={href} class="inline-flex items-center gap-1 text-sm text-muted hover-underline mb-space-4">
  &larr; {label}
</a>
